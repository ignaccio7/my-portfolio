---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;

---

<div class={`tag absolute bottom-0 left-0 border-t-2 border-r-2 border-third pt-2 pr-2 rounded-tr-md bg-third ${className}`}>
  <slot />
</div>